# Шаблон для .env — скопіюй у .env та підлаштуй за потреби.
# SQLite-файл відносно теки prisma/ — створюється при першій міграції.
DATABASE_URL="file:./dev.db"

# Опціонально: порт сервера (за замовчуванням 3000)
# PORT=3000
